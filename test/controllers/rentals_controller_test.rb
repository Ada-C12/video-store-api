require "test_helper"

describe RentalsController do
  describe "checkout" do 

    # it "should respond with JSON, created, & store a rental in the db for an existing customer and movie" do
    #   checkout_params = { customer_id: customers(:shelley), movie_id: movies(:first) }

    #   post 'rentals/check-out', params: checkout_params

    #   expect(response.header['Content-Type']).must_include 'json'
    #   must_respond_with :ok
    # end
  end

  describe "checkin" do
  end
end
